apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: washu-course-scheduler-ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: frontend-service
                port:
                  number: 3000
